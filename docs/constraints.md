# Constraints & Limitations

- HTML responses only; non-HTML content yields empty meta.
- Bots may be blocked by WAFs or robots rules.
- Only three crawler presets are supported.

Performance notes: dominated by network latency and redirect hops.
